services:
  website:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: chordaether-website
    restart: unless-stopped

    ports:
      - "8085:80"

    labels:
      - "chordaether.deployment-version=1"